# digit-identifier
small educational neural network for classifying MNIST handwritten digits

## What’s in this repo
- `digit_classification.py` — the `Network` class and training/evaluation code.
- `data_loader.py` — helper that loads/wraps data for training.
- `digit_gui.py` — GUI which allows testing of individual digits via direct user input
